[package]
name = "docker-registry"
version = "0.1.0"
edition = "2021"

[dependencies]
axum = "0.7"
tokio = { version = "1", features = ["full"] }
hyper = "1"
serde_json = "1.0"
serde = { version = "1.0", features = ["derive"] }